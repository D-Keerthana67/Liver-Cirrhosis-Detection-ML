# Notebooks

This directory is reserved for optional exploratory analysis and visualization notebooks.

The main project workflow is implemented as Python scripts in `src/` so that training and evaluation can be reproduced from the command line.
